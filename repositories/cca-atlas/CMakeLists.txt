cmake_minimum_required(VERSION 3.28)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(cca_atlas LANGUAGES NONE)
endif()

message(STATUS "cca-atlas is reserved for a future milestone; no targets are defined")
