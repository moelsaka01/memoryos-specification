# MO-1305 host interruption validity and selective continuation

The authorized 2026-09-24 host-interruption resume supplements verification correction V (`75cee55784c590bab52feaf8f2214e4d1b9e657f`). It changes measurement validity and modular evidence tooling. It does not change the gateway runtime, semantic ceiling, memory ceilings, frozen 15-vector selection or 10 cold / 20 warm requirements. R1, R4, R5 and R6 remain immutable failed attempts.

The local Windows System log is queried by explicit UTC `FilterXml`, using the Kernel-Power provider. This avoids the local-time ambiguity found in the earlier diagnostic's `FilterHashtable` query. The bounded query retains raw XML, record IDs and query interval. Errors, truncated queries, malformed events, missing data and inconsistent clocks cannot establish host interruption.

A matched 506 entry and 507 exit must share BootId and ScenarioInstanceIdV2, have increasing event record IDs, `SleepEntered=true`, positive integer SleepDurationInUs, and a total DurationInUs consistent with the entry/exit timestamps within 250 ms. The sleep correlation interval ends at the exit event and starts SleepDurationInUs before it. The actual R6 pattern is independently corroborated by event 566's previous sleep-session duration. The full OS duration is retained; it is never substituted for semantic duration.

A resource validity window runs from the pre-capture request cutoff through the post-capture acknowledgement; when an operation fails before the latter, its failure observation closes the window. A stress window runs from repetition start through cleanup completion. The window must have a single monotonic domain and UTC versus monotonic elapsed agreement within 250 ms. Sleep must intersect it by more than the 2 ms timestamp quantization margin. UTC serves only to correlate OS evidence. Operation metrics and all deadlines remain monotonic.

Only independently proven overlap yields `HOST_INTERRUPTED`. Such observations are retained, count neither as PASS nor as product FAIL, and contribute no maxima or valid sample count. Unknown evidence leaves the operation subject to ordinary assertions and resource limits. A valid 20-second semantic operation still derives an 80-second deadline and fails the unchanged 60-second ceiling.

Each interrupted logical resource slot receives a new observation identity. At most three replacements are allowed per slot and resource vector within a task; stress permits three per repetition. Exceeding the bound stops for host instability. Each attempt preserves candidate, logical coordinate, monotonic and UTC windows, raw events, provider/event/record identities, sleep interval, intersection, classification and any original failure.

R6 reuse independently verifies frozen source and input identities, runtime archive, contracts, schemas/OpenAPI, preliminary limits, clock and capture protocol, fixture bytes, HTTP status/body hash, per-process native capture rows, causal tickets, cleanup and retrospective OS evidence. The eleven completed vectors contribute 330 valid observations. The partial maximum-permitted-headers vector contributes ten cold observations and warm slot zero. Warm slot one and failed warm slot two overlap proven Modern Standby; both remain recorded. Completing a logical warm vector across retained and replacement process runs is authorized by this resume; every observation retains its actual process and session identity. This does not rewrite R6 or invent a contiguous run.

Immutable vector receipts bind exactly ten cold and twenty warm logical sample identities. The aggregate revalidates every member, recomputes the guard from raw OS evidence, checks membership uniqueness and recalculates maxima and the frozen variance rule. Only a triggered variance rule permits targeted extension. Final limits retain 4x semantic headroom with 100 ms upward rounding and 1.5x memory headroom with MiB upward rounding. Fixed limits, 24 MiB configured worker young generation and the 128 MiB young ceiling remain unchanged.

The final candidate changes only derived limits and their OpenAPI projection, with generated dependency/SBOM provenance. Required final acceptance includes 105 functional cases, 20 adverse functional scenarios and the frozen nine stress vectors at two ten-second repetitions, plus exact boundaries, installed clients, semantic parity, package/security gates, offline installation and the established dated supply-chain scope. Six existing regression groups exercise the SDK/semantic closure and transport/projection boundaries required by freeze section 12.3; unchanged C++/UI and hosted/certification suites are excluded.

The task uses a 45-minute target and hard 60-minute monotonic budget. Receipt completion is not Phase 1 completion until final acceptance, I1, B1 and the bounded post-B1 checks pass. Ubuntu, Linux, VM and cross-platform parity are NOT_REQUIRED. Phase 2 and Phase 3 remain gated until the appropriate predecessor gates complete.

The final acceptance launcher initially omitted Windows PowerShell from its restricted PATH. Its 20 adverse and 18 final stress repetitions retained query-UNAVAILABLE/NORMAL observations, with every ordinary assertion still enforced. The outer launcher now supplies the trusted Windows PowerShell directory. Separate immutable retrospective OS-event receipts revalidate those exact windows; the original observations are retained. Any proven overlap would require replacement of only that repetition before acceptance. This environmental query correction changes no measured runtime or sample data.
